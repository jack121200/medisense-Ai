fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# ML Core
scikit-learn==1.4.0
xgboost==2.0.3
scipy>=1.11.0

# Data Processing
pandas==2.1.4
numpy==1.26.3
openpyxl==3.1.2

# Model Persistence
joblib==1.3.2

# PDF + DOCX Extraction
pdfplumber>=0.10.0
python-docx>=1.1.0

# HTTP client
httpx==0.26.0
python-multipart>=0.0.6

# Bayesian network (bayesian_engine.py) — replaces hand-picked likelihood-ratio
# constants with CPDs actually fit on HeartDiseaseTrain-Test.csv
pgmpy==1.1.2

# ECG signal access for the deep-anomaly autoencoder (train/train_deep_anomaly.py)
# — reads directly from PhysioNet's open MIT-BIH Arrhythmia Database
wfdb==4.3.1

# 1D-CNN autoencoder for the ECG anomaly detector (deep_anomaly.py).
# CPU-only build — the Dockerfile passes --extra-index-url
# https://download.pytorch.org/whl/cpu when installing this file, since
# plain PyPI doesn't host the "+cpu" build. Installing this requirements.txt
# directly (e.g. local dev) needs the same flag:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0+cpu
